import 'package:flutter/material.dart';

class FaveScreen extends StatelessWidget {
  const FaveScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Fave Screen'),
    );
  }
}
